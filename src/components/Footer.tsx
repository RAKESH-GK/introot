export default function Footer() {
  return (
    <footer className="bg-gray-900 text-gray-400 text-center py-4 text-sm">
      © {new Date().getFullYear()} <span className="text-white font-semibold">Introot</span> — Built with ❤️ in Belthangady
    </footer>
  );
}
